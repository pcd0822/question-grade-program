[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA: 모든 경로를 index.html로 (함수 경로는 제외)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
